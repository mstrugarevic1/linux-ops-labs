diff --git a/lab05-mysql-contention/app.py b/lab05-mysql-contention/app.py
--- a/lab05-mysql-contention/app.py
+++ b/lab05-mysql-contention/app.py
@@ -29,5 +29,5 @@ run("UPDATE counters SET value = 0 WHERE id = 1")
 
 jobs = [
-    spawn("lock-holder", "START TRANSACTION; UPDATE counters SET value = value + 1 WHERE id = 1; SELECT SLEEP(600);")
+    spawn("lock-holder", "START TRANSACTION; UPDATE counters SET value = value + 1 WHERE id = 1; COMMIT; SELECT SLEEP(600);")
 ]
 time.sleep(1)
