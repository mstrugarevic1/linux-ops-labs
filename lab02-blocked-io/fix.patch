diff --git a/lab02-blocked-io/app.py b/lab02-blocked-io/app.py
--- a/lab02-blocked-io/app.py
+++ b/lab02-blocked-io/app.py
@@ -2,7 +2,7 @@ import os
 import subprocess
 import time
 
-WORKERS = 6
+WORKERS = 1
 BLOCKS = 512
 
 
@@ -10,7 +10,7 @@ def main():
     procs = []
     for n in range(WORKERS):
         path = f"/data/worker-{n}.bin"
-        cmd = f"while :; do dd if=/dev/zero of={path} bs=64K count={BLOCKS} oflag=sync conv=notrunc; done"
+        cmd = f"while :; do dd if=/dev/zero of={path} bs=64K count={BLOCKS} conv=notrunc; done"
         procs.append(subprocess.Popen(["sh", "-c", cmd], stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL))
     while True:
         running = sum(p.poll() is None for p in procs)
